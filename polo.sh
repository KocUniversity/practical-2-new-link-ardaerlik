#!/bin/bash

: '
no input
read the directory saved by the function marco 
and change to that directory
note that marco and polo should agree on the location where the directory is saved.
'

polo () {
  # read the content of the file using cat
  # and save it into a variable
  
  
  # change directory to the path stored in the variable
  
}
